## Utilisation d’alias de table

Les alias rendent les requêtes plus lisibles, surtout quand plusieurs tables sont impliquées.

select e.nom, e.lieu  
from evenement e;

- `e` est un alias pour `evenement`