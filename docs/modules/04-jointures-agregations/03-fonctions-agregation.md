Module fonctions d'agrégations

<!-- 
  IDÉE D'EXERCICE — à intégrer dans le lab des agrégations

  Afficher le prénom et nom de l'employé, le prénom et nom du client qu'il représente,
  et le total de chaque facture (sum(prix_unitaire * quantite)) pour ce client.
  Trier par nom d'employé.

  Nécessite : plusieurs jointures (employe → client → facture → ligne_facture)
  + SUM() + GROUP BY
-->